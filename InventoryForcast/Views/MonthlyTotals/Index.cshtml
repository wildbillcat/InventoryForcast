@model PagedList.IPagedList<InventoryForcast.Models.Calculations.MonthlyTotal>
@using PagedList.Mvc;

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<table class="table">
    <tr>
        <th>
            SKU
        </th>
        <th>
            Month_Id
        </th>
        <th>
            Quantity_Sold
        </th>
        <th>
            Absolute_Quantity_Sold
        </th>
        <th>
            Date
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SKU)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Month_Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quantity_Sold)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Absolute_Quantity_Sold)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>

Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount

@Html.PagedListPager(Model, page => Url.Action("Index", new { page }))
